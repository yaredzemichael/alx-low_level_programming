#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
        int x = 0;

        for (x = 0; x < 10; x++)
                printf("%d", x);

        printf("\n");

        return (0);
}
